* Crash (heap use-after-free) if worldserver fails to start due to missing world xml
* ERROR-trace if trying to look at a creature (it wants an item)
* Cannot move item to a tile where creature stands (note: isBlocking should only check items, another check for if a tile has creatures)
