dist: bionic
language: cpp

before_install:
  - sudo add-apt-repository ppa:mhier/libboost-latest -y
  - sudo apt update -q
  - sudo apt install clang clang-tidy libboost1.70-dev valgrind -y

jobs:
  include:
    - stage: build
      script: tools/travis/build.sh
    - stage: test
      script: tools/travis/test.sh
